# 多媒体处理流程

## 概述

File Organizer 2000 支持多种媒体文件类型的处理，包括 PDF、图像和音频文件。本文档详细说明各种媒体文件的处理流程。

## 支持的媒体类型

### 文件扩展名

| 类型 | 扩展名 | 处理方式 |
|------|--------|---------|
| 图像 | `.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`, `.svg` | 视觉识别/OCR |
| 音频 | `.mp3`, `.wav`, `.m4a`, `.ogg`, `.3gp`, `.flac` | 语音转文本 |
| PDF | `.pdf` | 文本提取 |

### 媒体文件标识

```typescript
const VALID_IMAGE_EXTENSIONS = ["jpg", "jpeg", "png", "gif", "webp", "svg"];
const VALID_AUDIO_EXTENSIONS = ["mp3", "wav", "m4a", "ogg", "3gp", "flac"];
const VALID_MEDIA_EXTENSIONS = [...VALID_IMAGE_EXTENSIONS, ...VALID_AUDIO_EXTENSIONS];

function shouldCreateMarkdownContainer(file: TFile): boolean {
  return VALID_MEDIA_EXTENSIONS.includes(file.extension) || file.extension === "pdf";
}
```

## 媒体文件处理架构

```mermaid
flowchart TB
    Start(["媒体文件添加"]) --> Detect{"检测文件类型"}
    
    Detect -->|"PDF"| PDFFlow["PDF 处理流程"]
    Detect -->|"图像"| ImageFlow["图像处理流程"]
    Detect -->|"音频"| AudioFlow["音频处理流程"]
    
    PDFFlow --> CreateContainer1["创建 MD 容器"]
    ImageFlow --> CreateContainer2["创建 MD 容器"]
    AudioFlow --> CreateContainer3["创建 MD 容器"]
    
    CreateContainer1 --> MoveAttachment1["移动到附件文件夹"]
    CreateContainer2 --> MoveAttachment2["移动到附件文件夹"]
    CreateContainer3 --> MoveAttachment3["移动到附件文件夹"]
    
    MoveAttachment1 --> Extract1["提取 PDF 文本"]
    MoveAttachment2 --> Extract2["生成图像描述"]
    MoveAttachment3 --> Extract3["转录音频"]
    
    Extract1 --> WriteContainer1["写入容器文件"]
    Extract2 --> WriteContainer2["写入容器文件"]
    Extract3 --> WriteContainer3["写入容器文件"]
    
    WriteContainer1 --> Continue["继续通用处理流程"]
    WriteContainer2 --> Continue
    WriteContainer3 --> Continue
    
    Continue --> Classification["文档分类"]
    Classification --> Folder["文件夹推荐"]
    Folder --> Rename["文件重命名"]
    Rename --> Format["内容格式化"]
    Format --> AppendMedia["附加媒体链接"]
    AppendMedia --> Tags["标签推荐"]
    Tags --> Done(["完成"])
    
    style Start fill:#e1f5e1
    style Done fill:#d4edda
```

## PDF 处理流程

### 流程图

```mermaid
flowchart TD
    Start(["PDF 文件"]) --> LoadPDFJS["加载 PDF.js 库"]
    LoadPDFJS --> ReadBinary["读取二进制数据"]
    ReadBinary --> ConvertBytes["转换为 Uint8Array"]
    
    ConvertBytes --> LoadDoc["加载 PDF 文档"]
    LoadDoc --> GetPageCount["获取页数"]
    
    GetPageCount --> InitText["初始化文本变量"]
    InitText --> LoopPages["循环处理页面"]
    
    LoopPages --> CheckPage{"当前页 <= 10?"}
    CheckPage -->|"否"| ReturnText["返回提取的文本"]
    CheckPage -->|"是"| GetPage["获取页面"]
    
    GetPage --> GetTextContent["获取文本内容"]
    GetTextContent --> ExtractText["提取文本项"]
    
    ExtractText --> JoinText["连接文本"]
    JoinText --> AppendText["追加到总文本"]
    
    AppendText --> NextPage["下一页"]
    NextPage --> CheckPage
    
    ReturnText --> WriteToContainer["写入容器文件"]
    WriteToContainer --> Done(["完成"])
    
    LoadDoc -->|"错误"| CatchError["捕获错误"]
    GetPage -->|"错误"| CatchError
    GetTextContent -->|"错误"| CatchError
    
    CatchError --> LogError["记录错误"]
    LogError --> ReturnEmpty["返回空字符串"]
    ReturnEmpty --> Done
    
    style Start fill:#e1f5e1
    style Done fill:#d4edda
    style CatchError fill:#f8d7da
```

### 详细步骤

#### 1. 加载 PDF.js

```typescript
const pdfjsLib = await loadPdfJs();
```

**说明：**
- 使用 Obsidian 内置的 PDF.js 库
- 异步加载确保库可用

#### 2. 读取 PDF 文件

```typescript
const arrayBuffer = await this.app.vault.readBinary(file);
const bytes = new Uint8Array(arrayBuffer);
```

**说明：**
- 使用 `readBinary` 读取二进制数据
- 转换为 Uint8Array 供 PDF.js 使用

#### 3. 加载文档

```typescript
const doc = await pdfjsLib.getDocument({ data: bytes }).promise;
```

**说明：**
- 创建 PDF 文档对象
- 异步加载文档结构

#### 4. 提取文本

```typescript
let text = "";
for (let pageNum = 1; pageNum <= Math.min(doc.numPages, 10); pageNum++) {
  const page = await doc.getPage(pageNum);
  const textContent = await page.getTextContent();
  text += textContent.items.map(item => item.str).join(" ");
}
```

**说明：**
- 只处理前 10 页（性能优化）
- 逐页提取文本内容
- 使用空格连接文本项

#### 5. 错误处理

```typescript
try {
  // PDF 处理逻辑
} catch (error) {
  logger.error(`Error extracting text from PDF: ${error}`);
  return "";
}
```

### PDF 处理示例

```mermaid
sequenceDiagram
    participant File as PDF 文件
    participant Plugin as FileOrganizer
    participant PDFJS as PDF.js
    participant Vault as Obsidian Vault
    
    Plugin->>Vault: readBinary(file)
    Vault-->>Plugin: ArrayBuffer
    
    Plugin->>PDFJS: loadPdfJs()
    PDFJS-->>Plugin: pdfjsLib
    
    Plugin->>PDFJS: getDocument(bytes)
    PDFJS-->>Plugin: PDF 文档对象
    
    loop 每一页 (最多10页)
        Plugin->>PDFJS: getPage(pageNum)
        PDFJS-->>Plugin: 页面对象
        
        Plugin->>PDFJS: getTextContent()
        PDFJS-->>Plugin: 文本内容
        
        Plugin->>Plugin: 提取并追加文本
    end
    
    Plugin->>Vault: modify(containerFile, text)
    Vault-->>Plugin: 写入成功
```

## 图像处理流程

### 流程图

```mermaid
flowchart TD
    Start(["图像文件"]) --> ReadBinary["读取二进制数据"]
    ReadBinary --> CreateBuffer["创建 Buffer"]
    CreateBuffer --> CheckSize["检查文件大小"]
    
    CheckSize --> CheckWebP{"是 WebP 格式?"}
    
    CheckWebP -->|"是"| UseOriginal["使用原始文件"]
    CheckWebP -->|"否"| LoadJimp["加载 Jimp"]
    
    LoadJimp --> ReadImage["读取图像"]
    ReadImage --> CheckDimensions{"尺寸 > 1000px?"}
    
    CheckDimensions -->|"是"| ResizeImage["缩放到 1000x1000"]
    CheckDimensions -->|"否"| KeepSize["保持原尺寸"]
    
    ResizeImage --> ConvertPNG["转换为 PNG"]
    KeepSize --> ConvertPNG
    
    ConvertPNG --> GetBuffer["获取 Buffer"]
    GetBuffer --> ToBase64["转换为 Base64"]
    
    UseOriginal --> ToBase64
    
    ToBase64 --> CallVisionAPI["调用视觉 API"]
    
    subgraph VisionAPI ["视觉 API"]
        CallVisionAPI --> Endpoint["POST /api/vision"]
        Endpoint --> Headers["设置请求头"]
        Headers --> Body["请求体"]
        
        Body --> Base64Image["image: base64Image"]
        Body --> Instructions["instructions: imageInstructions"]
        
        Base64Image --> SendRequest["发送请求"]
        Instructions --> SendRequest
    end
    
    SendRequest --> CheckStatus{"状态码 200?"}
    CheckStatus -->|"否"| APIError["API 错误"]
    CheckStatus -->|"是"| ParseResponse["解析响应"]
    
    ParseResponse --> ExtractText["提取文本描述"]
    ExtractText --> WriteToContainer["写入容器文件"]
    
    WriteToContainer --> Done(["完成"])
    APIError --> Done
    
    ReadImage -->|"错误"| ImageError["图像处理错误"]
    ImageError --> Done
    
    style Start fill:#e1f5e1
    style Done fill:#d4edda
    style VisionAPI fill:#f0f0f0
    style APIError fill:#f8d7da
    style ImageError fill:#f8d7da
```

### 详细步骤

#### 1. 读取图像

```typescript
const arrayBuffer = await this.app.vault.readBinary(file);
const fileContent = Buffer.from(arrayBuffer);
```

#### 2. 检查 WebP 格式

```typescript
function isWebP(fileContent: Buffer): boolean {
  return (
    fileContent.slice(0, 4).toString("hex") === "52494646" &&
    fileContent.slice(8, 12).toString("hex") === "57454250"
  );
}
```

**说明：**
- WebP 文件签名：`RIFF....WEBP`
- 检查文件头的十六进制值

#### 3. 压缩图像

```typescript
async function compressImage(fileContent: Buffer): Promise<Buffer> {
  const image = await Jimp.read(fileContent);
  
  if (image.getWidth() > 1000 || image.getHeight() > 1000) {
    image.scaleToFit(1000, 1000);  // 保持宽高比
  }
  
  const resizedImage = await image.getBufferAsync(Jimp.MIME_PNG);
  return resizedImage;
}
```

**优化策略：**
- 最大尺寸：1000x1000 像素
- 保持宽高比
- 转换为 PNG 格式

#### 4. 转换为 Base64

```typescript
const base64Image = arrayBufferToBase64(processedArrayBuffer);
```

#### 5. 调用视觉 API

```typescript
const response = await fetch(`${this.getServerUrl()}/api/vision`, {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    Authorization: `Bearer ${this.settings.API_KEY}`,
  },
  body: JSON.stringify({
    image: base64Image,
    instructions: this.settings.imageInstructions,
  }),
});

const { text } = await response.json();
```

### 图像处理示例

```mermaid
sequenceDiagram
    participant File as 图像文件
    participant Plugin as FileOrganizer
    participant Jimp as Jimp 库
    participant API as 视觉 API
    participant Vault as Obsidian Vault
    
    Plugin->>Vault: readBinary(file)
    Vault-->>Plugin: ArrayBuffer
    
    Plugin->>Plugin: 检查是否 WebP
    
    alt 非 WebP 格式
        Plugin->>Jimp: read(buffer)
        Jimp-->>Plugin: 图像对象
        
        alt 尺寸 > 1000px
            Plugin->>Jimp: scaleToFit(1000, 1000)
            Jimp-->>Plugin: 调整后的图像
        end
        
        Plugin->>Jimp: getBufferAsync(PNG)
        Jimp-->>Plugin: PNG Buffer
    end
    
    Plugin->>Plugin: 转换为 Base64
    
    Plugin->>API: POST /api/vision
    Note over Plugin,API: {" image: base64, instructions "}
    
    API-->>Plugin: {" text: "图像描述" "}
    
    Plugin->>Vault: modify(containerFile, text)
    Vault-->>Plugin: 写入成功
```

## 音频处理流程

### 流程图

```mermaid
flowchart TD
    Start(["音频文件"]) --> ReadBinary["读取二进制数据"]
    ReadBinary --> CreateFormData["创建 FormData"]
    
    CreateFormData --> AddAudio["添加音频 Blob"]
    AddAudio --> AddExtension["添加文件扩展名"]
    
    AddExtension --> CallTranscribeAPI["调用转录 API"]
    
    subgraph TranscribeAPI ["转录 API"]
        CallTranscribeAPI --> Endpoint["POST /transcribe"]
        Endpoint --> Headers["设置请求头"]
        Headers --> Auth["Authorization: Bearer token"]
        
        Auth --> SendFormData["发送 FormData"]
    end
    
    SendFormData --> CheckStatus{"状态码 200?"}
    CheckStatus -->|"否"| ParseError["解析错误"]
    ParseError --> ThrowError["抛出错误"]
    
    CheckStatus -->|"是"| GetReader["获取 ReadableStream Reader"]
    GetReader --> CreateDecoder["创建 TextDecoder"]
    
    CreateDecoder --> StreamLoop["流式读取"]
    
    subgraph Streaming ["流式处理"]
        StreamLoop --> ReadChunk{"读取 Chunk"}
        ReadChunk -->|"done"| StreamDone["流结束"]
        ReadChunk -->|"有数据"| DecodeChunk["解码 Chunk"]
        
        DecodeChunk --> YieldText["返回文本片段"]
        YieldText --> WriteToContainer["追加到容器文件"]
        WriteToContainer --> ReadChunk
    end
    
    StreamDone --> Done(["完成"])
    ThrowError --> Error(["错误"])
    
    style Start fill:#e1f5e1
    style Done fill:#d4edda
    style Error fill:#f8d7da
    style TranscribeAPI fill:#f0f0f0
    style Streaming fill:#f0f0f0
```

### 详细步骤

#### 1. 读取音频文件

```typescript
const audioBuffer = await this.app.vault.readBinary(file);
```

#### 2. 创建 FormData

```typescript
const formData = new FormData();
const blob = new Blob([audioBuffer], { type: `audio/${fileExtension}` });
formData.append("audio", blob, `audio.${fileExtension}`);
formData.append("fileExtension", fileExtension);
```

#### 3. 调用转录 API

```typescript
const transcribeUrl = "https://file-organizer-2000-audio-transcription.onrender.com/transcribe";

const response = await fetch(transcribeUrl, {
  method: "POST",
  body: formData,
  headers: {
    Authorization: `Bearer ${this.settings.API_KEY}`,
  },
});
```

**转录服务器：**
- 生产环境：`https://file-organizer-2000-audio-transcription.onrender.com/transcribe`
- 本地开发：`http://localhost:3001/transcribe`

#### 4. 流式接收转录结果

```typescript
const reader = response.body.getReader();
const decoder = new TextDecoder();

async function* generateTranscript() {
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    yield decoder.decode(value);
  }
}

return generateTranscript();
```

#### 5. 追加到容器文件

```typescript
async function appendTranscriptToActiveFile(
  parentFile: TFile,
  audioFileName: string,
  transcriptIterator: AsyncIterableIterator<string>
) {
  const transcriptHeader = `\n\n## Transcript for ${audioFileName}\n\n`;
  await this.app.vault.append(parentFile, transcriptHeader);
  
  for await (const chunk of transcriptIterator) {
    await this.app.vault.append(parentFile, chunk);
  }
  
  new Notice(`Transcription completed for ${audioFileName}`, 5000);
}
```

### 音频处理示例

```mermaid
sequenceDiagram
    participant File as 音频文件
    participant Plugin as FileOrganizer
    participant API as 转录 API
    participant Vault as Obsidian Vault
    
    Plugin->>Vault: readBinary(file)
    Vault-->>Plugin: ArrayBuffer
    
    Plugin->>Plugin: 创建 FormData
    Note over Plugin: {" audio: Blob, fileExtension "}
    
    Plugin->>API: POST /transcribe
    Note over Plugin,API: FormData with audio
    
    API-->>Plugin: ReadableStream (开始)
    
    loop 流式接收
        API-->>Plugin: 文本片段
        Plugin->>Vault: append(containerFile, chunk)
        Vault-->>Plugin: 追加成功
        Plugin->>Plugin: 显示进度
    end
    
    API-->>Plugin: 流结束
    Plugin->>Plugin: 显示完成通知
```

## 容器文件管理

### 容器创建流程

```mermaid
flowchart TD
    Start(["媒体文件"]) --> CheckType{"文件类型"}
    
    CheckType -->|"PDF"| CreatePDFContainer["创建 PDF 容器"]
    CheckType -->|"图像"| CreateImageContainer["创建图像容器"]
    CheckType -->|"音频"| CreateAudioContainer["创建音频容器"]
    
    CreatePDFContainer --> ContainerName1["文件名.md"]
    CreateImageContainer --> ContainerName2["文件名.md"]
    CreateAudioContainer --> ContainerName3["文件名.md"]
    
    ContainerName1 --> CreateFile1["创建空白 MD 文件"]
    ContainerName2 --> CreateFile2["创建空白 MD 文件"]
    ContainerName3 --> CreateFile3["创建空白 MD 文件"]
    
    CreateFile1 --> MoveOriginal1["移动原文件到附件"]
    CreateFile2 --> MoveOriginal2["移动原文件到附件"]
    CreateFile3 --> MoveOriginal3["移动原文件到附件"]
    
    MoveOriginal1 --> ExtractContent1["提取内容"]
    MoveOriginal2 --> ExtractContent2["提取内容"]
    MoveOriginal3 --> ExtractContent3["提取内容"]
    
    ExtractContent1 --> WriteContent1["写入容器"]
    ExtractContent2 --> WriteContent2["写入容器"]
    ExtractContent3 --> WriteContent3["写入容器"]
    
    WriteContent1 --> ProcessPipeline["进入处理管线"]
    WriteContent2 --> ProcessPipeline
    WriteContent3 --> ProcessPipeline
    
    ProcessPipeline --> AppendLink["追加媒体链接"]
    AppendLink --> FinalContainer["容器文件包含:\n- 提取的内容\n- 媒体文件链接"]
    
    FinalContainer --> Done(["完成"])
    
    style Start fill:#e1f5e1
    style Done fill:#d4edda
    style FinalContainer fill:#d4edda
```

### 容器文件结构

#### PDF 容器示例

```markdown
[提取的 PDF 文本内容]

---
分类：报告
标签：#finance #quarterly
---

![[report.pdf]]
```

#### 图像容器示例

```markdown
[AI 生成的图像描述]

这张图片显示了一个美丽的日落场景，天空呈现出橙色和粉色的渐变...

---
分类：照片
标签：#landscape #sunset
---

![[sunset.jpg]]
```

#### 音频容器示例

```markdown
## Transcript for interview.mp3

[流式转录的文本内容]

Speaker: Hello, welcome to today's podcast...
[更多转录内容...]

---
分类：采访
标签：#podcast #interview
---

![[interview.mp3]]
```

## 媒体文件并发控制

### 并发限制

```typescript
const MAX_CONCURRENT_MEDIA_TASKS = 2;
```

**原因：**
- 媒体文件处理消耗大量资源
- API 调用可能较慢（尤其是音频转录）
- 避免占用过多系统资源

### 媒体队列管理

```mermaid
stateDiagram-v2
    ["*"] --> MediaQueue: 媒体文件入队
    
    MediaQueue --> CheckSlots: 检查槽位
    
    CheckSlots --> Process: 槽位可用 (< 2)
    CheckSlots --> WaitingQueue: 槽位已满
    
    Process --> Active: 开始处理
    Active --> Completed: 处理完成
    
    Completed --> ReleaseSlot: 释放槽位
    ReleaseSlot --> CheckWaiting: 检查等待队列
    
    CheckWaiting --> ProcessNext: 有等待文件
    CheckWaiting --> ["*"]: 无等待文件
    
    ProcessNext --> Process
    
    WaitingQueue --> CheckWaiting: 等待槽位释放
```

### 队列处理逻辑

```typescript
private async processNextMediaFile(): Promise<void> {
  // 检查是否有等待的媒体文件和可用槽位
  if (
    this.mediaQueue.length === 0 ||
    this.activeMediaTasks >= MAX_CONCURRENT_MEDIA_TASKS
  ) {
    return;
  }
  
  // 从队列中取出下一个文件
  const nextFile = this.mediaQueue.shift();
  if (nextFile) {
    const hash = this.idService.generateFileHash(nextFile);
    this.queue.add(nextFile, { metadata: { hash } });
  }
}
```

## 错误处理

### PDF 错误处理

```mermaid
flowchart TD
    Start(["开始 PDF 处理"]) --> Try["尝试处理"]
    Try --> LoadDoc["加载文档"]
    
    LoadDoc -->|"成功"| ExtractPages["提取页面"]
    LoadDoc -->|"失败"| CatchError["捕获错误"]
    
    ExtractPages -->|"成功"| ReturnText["返回文本"]
    ExtractPages -->|"失败"| CatchError
    
    CatchError --> LogError["记录错误"]
    LogError --> ReturnEmpty["返回空字符串"]
    
    ReturnText --> Done(["完成"])
    ReturnEmpty --> Done
    
    style Start fill:#e1f5e1
    style Done fill:#d4edda
    style CatchError fill:#f8d7da
```

### 图像错误处理

```mermaid
flowchart TD
    Start(["开始图像处理"]) --> Try["尝试处理"]
    Try --> Compress["压缩图像"]
    
    Compress -->|"成功"| ToBase64["转换 Base64"]
    Compress -->|"失败"| CatchError["捕获错误"]
    
    ToBase64 --> CallAPI["调用 API"]
    
    CallAPI -->|"成功"| ReturnText["返回描述"]
    CallAPI -->|"失败"| APIError["API 错误"]
    
    CatchError --> LogError["记录错误"]
    APIError --> LogError
    
    LogError --> ThrowError["抛出错误"]
    ThrowError --> MoveToError["移动到错误文件夹"]
    
    ReturnText --> Done(["完成"])
    MoveToError --> Done
    
    style Start fill:#e1f5e1
    style Done fill:#d4edda
    style CatchError fill:#f8d7da
    style APIError fill:#f8d7da
```

### 音频错误处理

```mermaid
flowchart TD
    Start(["开始音频处理"]) --> Try["尝试处理"]
    Try --> ReadAudio["读取音频"]
    
    ReadAudio -->|"成功"| CallAPI["调用转录 API"]
    ReadAudio -->|"失败"| CatchError["捕获错误"]
    
    CallAPI -->|"成功"| StreamTranscript["流式接收"]
    CallAPI -->|"失败"| APIError["API 错误"]
    
    StreamTranscript -->|"成功"| ReturnIterator["返回迭代器"]
    StreamTranscript -->|"失败"| StreamError["流错误"]
    
    CatchError --> LogError["记录错误"]
    APIError --> LogError
    StreamError --> LogError
    
    LogError --> ShowNotice["显示错误通知"]
    ShowNotice --> ThrowError["抛出错误"]
    ThrowError --> MoveToError["移动到错误文件夹"]
    
    ReturnIterator --> Done(["完成"])
    MoveToError --> Done
    
    style Start fill:#e1f5e1
    style Done fill:#d4edda
    style CatchError fill:#f8d7da
    style APIError fill:#f8d7da
    style StreamError fill:#f8d7da
```

## 性能优化

### PDF 优化

| 优化项 | 策略 | 效果 |
|--------|------|------|
| 页数限制 | 只处理前 10 页 | 减少处理时间 |
| 异步加载 | 使用 Promise | 避免阻塞 |
| 错误恢复 | 单页失败不影响其他页 | 提高鲁棒性 |

### 图像优化

| 优化项 | 策略 | 效果 |
|--------|------|------|
| 尺寸压缩 | 最大 1000x1000 | 减少 API 成本 |
| WebP 检测 | 直接使用 WebP | 避免不必要的转换 |
| 格式标准化 | 统一转换为 PNG | 兼容性更好 |

### 音频优化

| 优化项 | 策略 | 效果 |
|--------|------|------|
| 流式处理 | 实时接收和显示 | 改善用户体验 |
| 并发限制 | 最多 2 个音频任务 | 避免资源耗尽 |
| 进度反馈 | 逐步追加文本 | 用户可见进度 |

## 配置选项

### 图像处理配置

```typescript
interface ImageSettings {
  imageInstructions: string;  // 图像描述指令
  // 例如："Describe this image in detail, focusing on key elements and text."
}
```

### API 端点配置

```typescript
// 视觉 API
const visionEndpoint = `${serverUrl}/api/vision`;

// 音频转录 API
const transcribeEndpoint = "https://file-organizer-2000-audio-transcription.onrender.com/transcribe";
```

## 总结

多媒体处理采用了：
- ✅ 统一的容器文件机制 - 所有媒体文件都创建 Markdown 容器
- ✅ 类型特定的处理流程 - PDF、图像、音频各有专门的处理逻辑
- ✅ 流式处理 - 音频转录支持实时显示
- ✅ 智能优化 - 图像压缩、PDF 页数限制
- ✅ 并发控制 - 限制媒体文件同时处理数量
- ✅ 错误恢复 - 完善的错误处理和回退机制
- ✅ 用户体验 - 实时反馈和进度显示
